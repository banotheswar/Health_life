export const facilityCallBack=((payload)=>({
    type:"Facility",
    payload:payload
}))
export const roomCallBack=((payload)=>({
    type:"Room",
    payload:payload
}))
export const specialityCallBack=((payload)=>({
    type:"Speciality",
    payload:payload
}))
export const doctorCallBack=((payload)=>({
    type:"Doctor",
    payload:payload
}))
export const availabilityCallBack=((payload)=>({
    type:"Availability",
    payload:payload
}))
